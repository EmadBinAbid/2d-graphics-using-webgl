/**
 * File:        math-toolkit.js
 * Author:      Anonymous
 * Created On:  15/09/2019
 * Description: /////////////////// 
*/

class MathToolkit {
    constructor() {

    }

    static square(number) {
        return Math.pow(number, 2);
    }

    static cube(number) {
        return Math.pow(number, 3);
    }

    static cube(number) {
        return Math.pow(number, 3);
    }

    // static consecutiveAddition(number) {
    //     returnedNumber = 0;
    //     while (number >= 0) {
    //         returnedNumber += number;
    //         number -= 1;
    //     }
    //     return returnedNumber;
    // }
}